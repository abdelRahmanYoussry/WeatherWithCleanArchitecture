class Weather{
  final int id;
  final String name;
  final String main;
  final String description;
  final int pressure;

  Weather(this.id, this.name, this.main, this.description, this.pressure);
}